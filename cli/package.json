{
  "name": "@techysy/strava-panel",
  "version": "1.3.1",
  "description": "Strava 骑行数据面板 CLI — 托盘管理的骑行统计服务(Tray-managed Strava cycling dashboard)",
  "bin": {
    "strava-panel": "./cli.js",
    "sp": "./cli.js"
  },
  "files": [
    "cli.js",
    "src",
    "hooks",
    "app",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "node scripts/build-cli.js",
    "prepublishOnly": "npm run build",
    "postinstall": "node hooks/postinstall.js || exit 0",
    "pack:cli": "npm run build && npm pack --pack-destination ../..",
    "publish:cli": "npm run build && npm publish --registry https://registry.npmjs.org"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "strava",
    "cycling",
    "dashboard",
    "tray",
    "python",
    "fitness"
  ],
  "license": "MIT",
  "author": "techysy",
  "repository": {
    "type": "git",
    "url": "https://github.com/techysy/strava-panel.git",
    "directory": "cli"
  },
  "comment_postinstall": "`|| exit 0` 是刻意的:该钩子只做环境检查与提示,失败不应中断整个 npm 安装。Strava Panel 是纯 Python 标准库服务,无 venv/pip 供给环节,首次 `sp start` 只需系统 Python 3.8+。",
  "comment_systray": "Windows 托盘用 PowerShell NotifyIcon(零二进制依赖,杀软友好);macOS/Linux 的 systray2 不打进包里,由 hooks/postinstall.js 惰性装到 ~/.strava-panel/runtime/node_modules,避免发布带未签名 Go 二进制触发杀软误报。"
}
